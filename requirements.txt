# Core scientific and audio processing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Deep learning & audio I/O
torch>=1.9.0
torchaudio>=0.9.0
torchcrepe>=0.0.14

# Audio feature extraction and metrics
librosa>=0.9.0
pysptk>=0.1.18
auraloss>=0.4.0
pesq>=0.0.4
fastdtw>=0.3.4

# visqol (not available via pip; requires manual build from Google's repository)
# cargan (not needed for the current implementation)